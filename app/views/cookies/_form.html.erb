<%= simple_form_for @cooky do |f| %>
  <% if cooky.errors.any? %>
    <div class="alert alert-danger">
      <h2><%= pluralize(cooky.errors.count, "error") %> prohibited this cooky from being saved:</h2>

      <ul>
      <% cooky.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.input :name %>
  <%= f.input :price %>
  <%= f.input :description %>
  <%= f.input :on_sale %>
  <%= f.button :submit, :class => "btn btn-success fullwidth" %>
<% end %>
